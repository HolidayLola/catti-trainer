# 技术文档：这个 CATTI Agent 是怎么造出来的

> 写给想自己动手做「人生支持系统」的你。看完这篇，你应该能理解每个零件为什么存在，
> 并且有能力照着做出减肥教练、Python 学习助手、解梦分析师……

---

## 一、先看全局：一个"每日陪练 Agent"由哪六个零件组成

任何"每天主动找你、跟你互动、越用越懂你"的系统，拆开都是这六件事：

```
① 闹钟          每天固定时间自动醒来           （GitHub Actions 定时任务）
② 获取素材      抓新闻/生成习题/查天气……       （Python 脚本 + 网络请求）
③ AI 加工       把素材变成为你定制的内容        （调用大模型 API）
④ 推送          把内容送到你眼前               （邮件 / 微信 / Telegram）
⑤ 交互界面      你在这里练习、打卡、提问        （网页，托管在 GitHub Pages）
⑥ 记忆回路      记录你的表现，喂给明天的 ①-③   （私密 Gist 当小数据库）
```

第 ⑥ 步是"辅导老师"和"闹钟"的区别：没有它，系统每天都把你当陌生人；
有了它，系统一天比一天懂你的短板。

本项目里这六个零件对应的文件：

| 零件 | 文件 | 作用 |
|---|---|---|
| ① 闹钟 | `.github/workflows/daily.yml` | 每天 UTC 22:45（北京 6:45）触发 |
| ②③ 素材+加工 | `scripts/daily.py` | 抓德语新闻 → 调 DeepSeek 出题 |
| ④ 推送 | `daily.py` 里的 `send_mail()` | QQ 邮箱 SMTP 发信 |
| ⑤ 界面 | `index.html` + `app.js` + `style.css` | 练习页（批改/词源/复习都在这） |
| ⑥ 记忆 | 私密 Gist + `daily.py` 里的 `fetch_profile()` | 学习记录 → 学员档案 → 定制出题 |

---

## 二、GitHub 托管是怎么回事（小白版）

你可以把 GitHub 理解成一个**免费送你三样东西**的地方：

### 1. 仓库（Repository）＝ 云端文件夹 + 时光机

`https://github.com/HolidayLola/catti-trainer` 就是你的仓库。
它保存全部代码和每天生成的材料（`data/` 目录），并且记住每一次修改（叫 commit），
改坏了随时可以回退。本地文件夹 `C:\Users\86195\catti-trainer` 和它通过 `git push` / `git pull` 同步。

### 2. GitHub Actions ＝ 免费的云端电脑 + 闹钟

`.github/workflows/daily.yml` 这个文件就是给云端电脑下的指令单：

```yaml
on:
  schedule:
    - cron: "45 22 * * *"    # ← 闹钟：UTC 时间每天 22:45（=北京 6:45）
  workflow_dispatch: {}       # ← 也允许手动点按钮触发

jobs:
  generate:
    runs-on: ubuntu-latest    # ← GitHub 免费借你一台 Linux 电脑
    steps:
      - uses: actions/checkout@v4        # 把仓库代码下载到这台电脑
      - uses: actions/setup-python@v5    # 装 Python
      - run: pip install requests        # 装依赖
      - run: python scripts/daily.py     # 跑我们的脚本（抓新闻、调AI、发邮件）
      - run: git add data/ && git commit && git push   # 把生成的材料存回仓库
```

关键点：
- **cron 表达式** `45 22 * * *` 五个位置分别是"分 时 日 月 星期"，`*` 表示每。
  注意它用 UTC 时间，北京时间要减 8 小时。
- 这台云端电脑在**国外**，访问 DW、Tagesschau 畅通无阻——这就是为什么
  "抓德语新闻"这一步必须放云端而不是你的电脑（国内网络访问不了 DW）。
- 公开仓库的 Actions **完全免费**，每天跑几分钟根本用不完额度。

### 3. GitHub Pages ＝ 免费网站托管

在仓库设置里开启 Pages 后，仓库根目录的 `index.html` 就变成了网站：
`https://holidaylola.github.io/catti-trainer/`。

它只能托管**静态文件**（HTML/CSS/JS），没有后端服务器——那"智能"从哪来？
诀窍是：**AI 调用发生在你的浏览器里**。`app.js` 直接从浏览器请求
`https://api.deepseek.com/chat/completions`，你的 API Key 存在浏览器本地
（localStorage），不经过任何中间服务器。所以整个系统没有一台需要花钱、
需要维护的服务器。

### 4. 两个配角

- **Secrets**（仓库设置 → Secrets and variables → Actions）：密钥保险柜。
  API Key、邮箱授权码放这里，Actions 运行时以环境变量注入，日志里自动打码，
  代码里永远不出现明文。本项目有 7 个：`DEEPSEEK_API_KEY`、`SMTP_HOST/PORT/USER/PASS`、
  `MAIL_TO`、`GIST_ID`。
- **Gist**（gist.github.com）：可以理解为"免费的单文件小数据库"。
  练习页把你的生词本、成绩写进一个私密 Gist（`catti-progress.json`），
  手机电脑同步，云端 Actions 每天早上也去读它——这就是记忆回路的载体。
  为什么不直接存进仓库？因为仓库是公开的，学习记录是隐私。

---

## 三、这个 Agent 的关键设计决策（踩坑记录，照抄可避坑）

1. **为什么不用服务器/不写后端？**
   个人工具最大的敌人是维护成本。静态网页 + 浏览器直连 LLM + Actions 定时任务，
   全套零成本零运维，坏了看一眼 Actions 日志就能定位。

2. **AI 出题必须防"幻觉"**。让模型"从新闻里摘一段"，它可能自己编。
   所以 `daily.py` 里有**逐字校验**：模型摘的段落必须能在原文里找到
   （`normalize(passage) in normalize(chosen["text"])`），验不过就用程序硬截取。
   **凡是要求"真实性"的内容，永远用代码校验，不要信模型的自觉。**

3. **推理模型的 token 坑**。deepseek-v4-pro 这类推理模型会先"内心思考"再回答，
   思考也消耗 max_tokens。给 20 就只够它思考、答案是空的。教训：
   给推理模型的 max_tokens 至少几千起步，且解析回复时要容错（用正则提取 JSON）。

4. **强弱模型分工**。批改用强模型（v4-pro，语法分析要可靠），
   查词源/问表达用快模型（v4-flash，1-3 秒出结果），查过的内容进本地缓存不再计费。
   **省钱 + 提速的通用套路：按任务难度路由模型 + 缓存。**

5. **每次调用只发当次需要的内容**，绝不携带全部历史。所以 token 消耗每天恒定，
   不会随使用时间"滚雪球"。个性化不是靠塞历史记录，而是靠**代码先做统计分析**
   （近 7 天均分、高频错误类型 Top5、薄弱词 Top15），把几百字的**摘要**发给模型。

6. **Windows 特色坑**：记事本保存的文本可能带 BOM（隐形字符），跟着密钥进了
   HTTP 头会报错——所以 `env()` 函数对所有环境变量做清洗。

7. **考试对齐**：批改 prompt 里写死了 CATTI 阅卷逻辑（扣分制、直译优先、
   意译按错译处理）。**做垂直领域 Agent，把领域规则写进 prompt 比换更强的模型有用得多。**

---

## 四、通用模板：做任何"人生支持系统"的六步法

你后面想做的每个 Agent，都往这张表里填空即可：

| 零件 | CATTI 陪练（本项目） | 减肥教练 | Python 学习助手 | 解梦/心理复盘 |
|---|---|---|---|---|
| ① 闹钟 | 每天 6:45 | 每天 6:00 + 睡前 22:00 | 每天 8:00 | 每天早上 7:30 |
| ② 素材 | 抓德语新闻 | 无需外部数据（AI 生成食谱/训练） | AI 按大纲生成习题 | 你昨晚记录的梦 + 白天日志 |
| ③ AI 加工 | 选段+出题+批改 | 按你的体重曲线和完成度排今日训练与三餐 | 讲解概念+出题+改代码 | 梦境主题分析+关联白天压力事件 |
| ④ 推送 | 邮件 | 邮件/微信 | 邮件 | 邮件 |
| ⑤ 界面 | 翻译+批改+复习页 | 打卡页：体重/饮食/训练完成度 | 在线做题+AI 判题页 | 记梦页+对话式复盘 |
| ⑥ 记忆 | 得分/错误类型/薄弱词 | 体重曲线、完成率、偏好食材 | 错题本、知识点掌握度 | 梦境主题词频、情绪趋势 |

动手顺序（每个新 Agent 都一样）：

1. 建仓库，先写 ⑤（一个能看的静态页）→ 开 Pages，马上就有成就感
2. 写 ②③（一个 Python 脚本，本地跑通：素材 → 调 AI → 输出 JSON 文件）
3. 加 ①④（workflow yml + SMTP 发信），配好 Secrets，手动触发测试
4. 页面读 JSON 展示每日内容，浏览器直连 LLM 做交互功能
5. 最后加 ⑥（Gist 存记录 → 脚本读记录 → 塞进出题 prompt）——自适应闭环
6. 让 Claude Code 帮你写：把这份文档给它看，说"照这个架构给我做个 XX Agent"

三点提醒：

- **隐私分级**。学习数据放私密 Gist 无所谓；但**解梦/心理类数据非常敏感**，
  建议整个仓库用私有仓库（Pages 需付费版，或界面改为本地 HTML 文件），
  至少绝不能把梦境日志放进公开仓库。另外心理分析 Agent 只能做"复盘助手"，
  它不是心理咨询的替代品，prompt 里要写明让它在发现持续低落时建议求助专业人士。
- **推送渠道可替换**。邮件最简单；想推微信可以用 PushPlus/Server酱（一个 HTTP
  请求就能发微信消息，把 `send_mail` 换成 `requests.post` 即可）；Telegram bot 也是一行请求。
- **多个 Agent 的整合**。架构相同的系统天然可以合并：一个仓库、多个 workflow
  （减肥的、学习的、心理的各自定时）、一个门户页面分几个 tab，记忆各存各的 Gist。
  等你做到第三个的时候，自然会看出怎么合。

---

## 五、本项目文件索引（想改哪里看哪里）

```
catti-trainer/
├── index.html            页面骨架（tab 导航、页头倒计时）
├── app.js                全部交互逻辑：AI 批改、词源查询、句子解析、
│                         间隔重复复习、小卷、Gist 同步、设置
├── style.css             样式（自动适配深色模式）
├── scripts/daily.py      云端每日脚本：抓新闻→读档案→AI 出题→存数据→发邮件
├── .github/workflows/daily.yml   定时任务定义
├── data/                 每天一个 JSON（当日全部材料），index.json 是目录
├── README.md             使用说明
└── 技术文档.md            本文档
```

常用运维命令（在本地项目文件夹执行）：

```bash
# 手动触发一次生成（比如想立刻看效果）
gh workflow run daily-material -R HolidayLola/catti-trainer

# 只重发今天的邮件（材料已存在时）
gh workflow run daily-material -R HolidayLola/catti-trainer -f force_mail=1

# 看最近一次运行是否成功
gh run list -R HolidayLola/catti-trainer --limit 3
```

出问题的排查顺序：GitHub 仓库 → Actions 标签页 → 点开红叉的运行 → 看日志里
最后的报错行。看不懂就把报错贴给 Claude。
